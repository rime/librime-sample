#!/bin/bash

if [[ "$OSTYPE" =~ darwin ]]; then
    echo "TODO: install the Rime plugin's dependencies on macOS"
elif [[ "$OSTYPE" =~ linux-gnu ]]; then
    echo "TODO: install the Rime plugin's dependencies on Linux"
fi
